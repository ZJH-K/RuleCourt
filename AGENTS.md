## Agent 技能

### Issue tracker（任务跟踪）

本仓库的 Issue 由 GitHub 仓库 `ZJH-K/RuleCourt` 跟踪。操作约定见 `docs/agents/issue-tracker.md`。

### Triage labels（分流标签）

使用五个默认分流标签。标签说明见 `docs/agents/triage-labels.md`。

### 领域文档

本仓库采用单一上下文的领域文档结构。说明见 `docs/agents/domain.md`。